# Frontend (Vite)
VITE_API_URL="http://localhost:3000"

# Backend (Express) — never use VITE_ prefix for secrets
ANTHROPIC_API_KEY="your_anthropic_api_key"
ANTHROPIC_MODEL="claude-haiku-4-5"
FRONTEND_ORIGIN="http://localhost:5173"
PORT=3000

# Contact form uses Netlify Forms (no EmailJS keys needed).
# After deploy: Netlify → Forms → enable email notification to serbelcloud@gmail.com
