[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback so deep links serve the Vite app
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
